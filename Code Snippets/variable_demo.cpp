/* Demonstrating how changing variable names change contexts */

//Until the player has exited the game, run the following code
While (game_is_on) {
	//Check if the player has removed a threat to U.S. safety
	if (enemy_killed) {
		//Increase the count of terrorists eliminated by one
		terrorists_eliminated++
	}
}

//Until the player has exited the game, run the following code
While (game_is_on) {
	//Check if the player has committed murder
	if (enemy_killed) {
		//Increase the count of families destroyed by one
		families_destroyed++
	}
}